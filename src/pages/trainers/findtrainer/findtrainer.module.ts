import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { FindtrainerPage } from './findtrainer';

@NgModule({
  declarations: [
    FindtrainerPage,
  ],
  imports: [
    IonicPageModule.forChild(FindtrainerPage),
  ],
})
export class FindtrainerPageModule {}
